# Rust
/target/
**/target/
**/*.rs.bk
Cargo.lock.bak

# Upstream Pascal source kept locally for cross-reference only.
# Not tracked in this repository (use the upstream distribution instead — see LICENSE).
/_forked/

# IDE / Editor
/.idea/
/.vscode/
/.vs/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db
desktop.ini

# Local sandbox launcher (環境固有のため除外。共有が必要なら個別に -f で add)
/claude_sandbox.bat

# Personal launcher (embeds the user's ARKSA_DIR path).
# Use run.example.ps1 as a template instead.
/run.ps1

# Local screenshots / scratch debugging artefacts.
/debugs/

# Local secrets
.env
.env.local
*.pem
*.pat
